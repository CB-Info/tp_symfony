<div class="card">
  <div class="card-body">
    <h5 class="card-title display-5">
      {{ data.results.0.title }}
    </h5>
    <p class="card-text">
      {{ data.results|length }} articles
    </p>
    <a href="{{ path('category_item', {'id': data.results}) }}" class="btn btn-primary">Voir</a>
  </div>
</div>